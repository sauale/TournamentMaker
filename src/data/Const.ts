const rootDirectory = "/";

export const URLMAP = {
  HOME: rootDirectory,
  MANAGE_TOURNAMENTS: `${rootDirectory}/manage-tournaments`,
  CREATE_TOURNAMENTS: `${rootDirectory}/create-tournaments`,
};
